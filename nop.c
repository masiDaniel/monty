#include "monty.h"

/**
 * _nop - does nothing substancial
 * @head: pointer to head of stack
 * @count: contains the numberr of lines
 * Return: nothing
 */

void _nop(stack_t **head, unsigned int count)
{
	(void) count;
	(void) head;
}
